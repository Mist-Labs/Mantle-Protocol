{
  "name": "@mantle/shadow-swap",
  "version": "1.0.0",
  "description": "Rust Actix Web backend service for the Mantle monorepo.",
  "main": "src/main.rs",
  "license": "MIT",
  "scripts": {
    "start": "cargo run --release",
    "dev": "cargo watch -x run",
    "build": "cargo build",
    "test": "cargo test",
    "lint": "cargo clippy -- -D warnings",
    "fmt": "cargo fmt -- --check",
    "check": "yarn fmt && yarn lint && yarn test"
  },
  "devDependencies": {
    "cargo-watch": "^8.5.2",
    "cargo-clippy": "^0.1.75",
    "cargo-expand": "^1.0.64"
  }
}